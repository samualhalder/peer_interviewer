export type FrontEndUser = {
  firstName: string;
  lastName: string;
  email: string;
  photoURL: string;
};
export type UserType = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  photoURL: string;
  password: string;
  created_at: Date;
};
